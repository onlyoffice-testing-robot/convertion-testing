    Need: **x2t** and libs **libDjVuFile.so**,
     **libdoctrenderer.so**,  **libHtmlFile.so**,
      **libHtmlRenderer.so**, **libicudata.so.55**,
       **libicuuc.so.55**, **libPdfReader.so**,
        **libPdfWriter.so**, **libUnicodeConverter.so**,
        **libXpsFile.so**.
